{ ... }:

{
  wayland.windowManager.hyprland.settings = {
    "$terminal" = "alacritty --title alacritty_float";
    "$fileManager" = "nemo";
    "$menu" = "wofi --show drun";
    "$browser" = "firefox";
  };
}
